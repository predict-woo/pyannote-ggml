{
  "name": "pyannote-cpp-node-workspace",
  "private": true,
  "version": "0.2.2",
  "type": "module",
  "engines": { "node": ">=18" },
  "scripts": {
    "build": "pnpm -r build",
    "test": "vitest run"
  },
  "devDependencies": {
    "vitest": "^3.0.0",
    "typescript": "^5.7.0"
  }
}
